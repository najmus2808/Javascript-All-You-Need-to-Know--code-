/** The valueOf() method returns the array itself.

valueOf() is the default method of the array object.

valueOf() does not change the original array. */

const fruits = ["Banana", "Orange", "Apple", "Mango"];
fruits.valueOf(); // Returns fruits

console.log(fruits);
