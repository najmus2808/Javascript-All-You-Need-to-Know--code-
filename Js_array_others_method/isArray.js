/** The isArray() method returns true if an object is an array, otherwise false. */

const fruits = ["Banana", "Orange", "Apple", "Mango"];
const result = Array.isArray(fruits); // Returns true
console.log(result);
